// next.config.js
const nextConfig = {
  distDir: 'out', // This specifies that the exported static files should go into the 'out' directory
  output: 'export', // Ensures Next.js will export the app as static HTML
};

module.exports = nextConfig;
