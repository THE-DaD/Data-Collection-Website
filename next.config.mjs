/** @type {import('next').NextConfig} */
const nextConfig = {
    distDir: '.next', // This tells Next.js to build to the '.next' directory
    // ... other configuration options
  };
  
  // Export using ES module syntax
  export default nextConfig;
  